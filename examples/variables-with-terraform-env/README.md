# Variables with Terraform Environment Variables Example

This example demonstrates how PlanCost respects Terraform environment variables.

It shows that variables set via `TF_VAR_name` environment variables (e.g., `TF_VAR_location`) are correctly picked up and used by PlanCost during estimation.
