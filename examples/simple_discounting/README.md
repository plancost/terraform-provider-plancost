# Simple Discounting Example

This example demonstrates how to apply discounts to cost estimates.

It uses the `discount` block within the `plancost_estimate` resource to apply a percentage discount to specific resource types (e.g., `azurerm_public_ip`), reflecting negotiated rates or other cost adjustments.
