# Variables with PlanCost Environment Variable Example

This example demonstrates how to specify a variable file using an environment variable.

It shows that you can use the `PLANCOST_VAR_FILE` environment variable to tell PlanCost which variable file to use (e.g., `tf.tfvars`), offering flexibility in CI/CD environments.
