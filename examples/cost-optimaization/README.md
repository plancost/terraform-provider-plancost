# Cost Optimization Example

This example demonstrates how to enable cost optimization recommendations in the `plancost_estimate` resource.

By setting `recommendations_enabled = true`, the provider will analyze the resources (like the Virtual Machine in this example) and provide recommendations to optimize costs.
