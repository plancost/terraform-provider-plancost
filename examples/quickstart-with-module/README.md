# Quickstart with Module Example

This example demonstrates how to use the `plancost_estimate` resource with a Terraform module.

It uses an Azure Verified Module (AVM) for Storage Account to show that PlanCost works seamlessly with resources created via modules.
