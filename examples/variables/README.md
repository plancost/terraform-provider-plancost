# Variables Example

This example demonstrates standard variable usage with PlanCost.

It shows how `plancost_estimate` works in a configuration that uses Terraform variables defined in `variables.tf` and values provided in `terraform.tfvars`.
