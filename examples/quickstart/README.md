# Quickstart Example

This is a basic example showing how to use the `plancost_estimate` resource with a simple Azure resource (Public IP).

It serves as a starting point for understanding how the PlanCost provider works.
